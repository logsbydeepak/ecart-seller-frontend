overwrite: true
schema: ${SCHEMA_PATH}
documents: "src/**/*.gql"
config:
  allowEnumStringTypes: true
generates:
  src/types/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
