mutation DeleteUser($currentPassword: String!) {
  deleteUser(currentPassword: $currentPassword) {
    ... on SuccessResponse {
      __typename
      message
    }
    ... on ErrorResponse {
      __typename
      title
      message
    }
  }
}
