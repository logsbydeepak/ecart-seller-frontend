mutation CreateSession($email: String!, $password: String!) {
  createSession(email: $email, password: $password) {
    ... on Token {
      __typename
      token
    }
    ... on ErrorResponse {
      __typename
      title
      message
    }
  }
}
