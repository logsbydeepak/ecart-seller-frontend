mutation DeleteAllSession($currentPassword: String!) {
  deleteAllSession(currentPassword: $currentPassword) {
    ... on SuccessResponse {
      message
    }
    ... on ErrorResponse {
      title
      message
    }
  }
}
